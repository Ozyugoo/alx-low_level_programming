#include <stdio.h>

/**
 * main - Output "with proper grammar, but the outcome is a piece of art,
 *
 * Return: Always 0
 */
i
int main(void)
{
	printf("\"with proper grammar, but the outcome is a piece of art,'\n'");
		return (0);
}
